'use strict';

module.exports = function(leds) {
    /*
    const beat = leds.beat % leds.pixels.length;

    const head = leds.pixels.slice(0, beat);
    const tail = leds.pixels.slice(beat);

    leds.pixels = tail.concat(head);
    */
};
