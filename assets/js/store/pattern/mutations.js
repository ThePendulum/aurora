'use strict';

export default {
    setRed(state, red) {
        state.red = red;
    },
    setGreen(state, green) {
        state.green = green;
    },
    setBlue(state, blue) {
        state.blue = blue;
    },
    setHue(state, hue) {
        state.hue = hue;
    },
    setSaturation(state, saturation) {
        state.saturation = saturation;
    },
    setValue(state, value) {
        state.value = value;
    },
    setSync(state, sync) {
        state.sync = sync;
    }
};
